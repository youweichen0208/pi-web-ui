# pi-web-ui

pi-web-ui（Pi 的 Web 与桌面聊天工作台，两端共用同一套 Web UI）的领域术语表。只记录本上下文特有的概念，不记录实现细节。

## Language

### 项目与列表

**项目（Project）**:
用户打开并切换的工作区，即一个文件目录；通过打开或切换动作首次进入项目列表。
_Avoid_: 工作区、Workspace、文件夹

**首次添加（First-added）**:
项目第一次进入项目列表的时刻，决定其固定位置：新进入的排最前，此后位置不再变化。
_Avoid_: lastUsed、最近使用、创建时间

**项目列表（Project List）**:
左侧面板的项目清单：按首次添加时间新→旧排列，条目位置不因选中或使用而移动，超上限时移除最早添加的。
_Avoid_: 最近项目、Recent projects

### 文件与对话

**当前文件（Current File）**:
预览面板中正在打开的文件；单文件模型，同一时刻只有一个。
_Avoid_: 活动文件、多标签

**编辑器快照（Editor Snapshot）**:
发送消息时随消息携带的当前文件编辑器内容，包含未保存的修改。
_Avoid_: 引用、文件内容

**当前文件标签（Current-file Chip）**:
输入框上方自动跟随当前文件的标签：始终只指向当前文件，随其打开与关闭出现和消失，发送时携带编辑器快照。
_Avoid_: 自动引用、路径引用

**发送即保存（Save-on-send）**:
发送携带当前文件的消息时，先把该文件的未保存修改存盘，使磁盘与快照一致。
_Avoid_: 自动保存、auto-save

**改动回流（Backfill）**:
agent 对当前打开文件的写入回流到编辑器：无未保存修改时自动重载，有则由用户二选一（载入新版或保留草稿）。
_Avoid_: 同步、合并

**对话（Session）**:
项目内与 AI 的一段对话；创建时间在创建之时写定，此后永不改写。
_Avoid_: 会话、chat、聊天记录

**历史对话（Session List）**:
左侧面板的对话清单：按创建时间新→旧排列，位置不因选中或发消息而移动。
_Avoid_: 最近对话、会话列表
